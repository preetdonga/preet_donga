#include <stdio.h>

void main() {
    int a,l,p;
    printf("enter value of side :");
    scanf("%d",&l);
    a=l*l;
    p=4*l;
    printf("Area of the square: %d\n", a);
    printf("Perimeter of the square: %d\n", p);
}
