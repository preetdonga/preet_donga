#include <stdio.h>

void main()
 {
    float a,h,l;
    printf("Enter the length of the rectangle: ");
    scanf("%f", &l);
    printf("Enter the high of the rectangle: ");
    scanf("%f", &h);
    a=(h*l)/2;
    printf("Area of the tirangle = %f", a);
}
